# Only needed for a static production build talking to a server that
# isn't on the same origin. In local dev, Vite's proxy (see
# vite.config.js) forwards /api to the Express server automatically and
# this can stay unset.
VITE_API_BASE_URL=http://localhost:4000
